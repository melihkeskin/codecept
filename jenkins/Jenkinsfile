 pipelin {
   agent any

   stages {
     stage("Initializing") {
       steps {
         sh 'npm install -y' 
       }
     }

     stage("Running Tests") {
       steps { 
           sh 'npx codeceptjs run --steps --plugins allure'
       }
     }
   }

 }