 pipeline {
   agent any

   tools {nodejs "build-default"}

   stages {
     stage("Initializing") {
       steps {
         sh 'npm install' 
       }
     }

     stage("Running Tests") {
       steps { 
           sh 'npx codeceptjs run --steps --plugins allure'
       }
     }
   }

 }